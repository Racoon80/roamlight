<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ground" />
    <foreground android:drawable="@drawable/ic_launcher_foreground" />
    <monochrome android:drawable="@drawable/ic_launcher_foreground" />
</adaptive-icon>
<!--
  ⚠ The folder has to keep its `-v26`, even though minSdk is already 26 and
    lint calls the qualifier unnecessary. Drop it and AAPT stops finding the
    icon at all: "resource mipmap/ic_launcher not found", and nothing builds.
    Tested, not guessed.
-->
